{% extends 'base.html.twig' %}

{% block title %}Accueil{% endblock %}

{% block body %}
  <div class="col-sm-7 mt-4 mx-4">
    {% for post in posts %}
    <div class="row post mb-1">
      <div class="col-12">
        <h2 class="text-capitalize">{{post.title}}</h2>
        <p class="added"><a href="#" class="username">Haris</a>{{post.postDate|date("F jS \\à g:ia")}}</p>
        <p>{{post.content}}</p>
        <a href="#" class="btn btn-default float-left text-capitalize">Lire la suite</a>
      </div>
    </div>
    {% endfor %}
  </div>
  <div class="col-sm-4 container col-12">
    {{include('includes/sidebar.html.twig')}}
  </div>




{% endblock %}
